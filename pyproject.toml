[project]
name = "paper-trading"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "shieric984@gmail.com" }
]
requires-python = ">=3.9"
dependencies = [
    "fastapi[standard]>=0.128.8",
]

[project.scripts]
paper-trading = "paper_trading:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"

[tool.fastapi]
entrypoint = "test:app"
